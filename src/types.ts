export interface Project {
    id: number;
    title: string;
    tags: string;
    started_date: string;
    finished_date: string;
    purpose: string;
    description: string;
    image: string;
    link: string;
}